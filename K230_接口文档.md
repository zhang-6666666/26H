# K230 → STM32 数据传输接口

## 硬件连接

| K230 UART2 | STM32 USART2 |
|------------|-------------|
| TX | PA3 (RX) |
| GND | GND |

## 通信参数

- 波特率：**115200**
- 数据位：8
- 停止位：1
- 校验：无
- 周期：**50ms** 发一次

## 数据格式

**2 字节，小端序 int16**

| 字节0 | 字节1 |
|-------|-------|
| pos[7:0] 低 8 位 | pos[15:8] 高 8 位 |

`pos`：钢球沿倾斜轴的水平位置，单位 **mm**，中心 = 0，左负右正。

## K230 发送代码

```python
from machine import UART
import time

uart = UART(2, baudrate=115200)

while True:
    pos = get_ball_position()              # 你的检测函数，返回 int
    uart.write(bytes([pos & 0xFF, (pos >> 8) & 0xFF]))
    time.sleep_ms(50)
```

## STM32 接收端

USART2 RXNE 中断，2 字节拼成 `camera.pos_mm`（int16_t），无帧头无校验。

## 示例数据

| min | hex | decimal |
|-----|-----|---------|
| -100 | `9C FF` | `0xFF9C` |
| -1 | `FF FF` | `0xFFFF` |
| 0 | `00 00` | `0x0000` |
| 1 | `01 00` | `0x0001` |
| 100 | `64 00` | `0x0064` |
